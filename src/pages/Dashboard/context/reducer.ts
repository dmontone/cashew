import { DashboardActions, DashboardState } from './types';

export const dashboardReducer: MapReducerType<DashboardState, DashboardActions> = (state, actions) => {
  switch (actions.type) {
    default:
      return state
  }
}